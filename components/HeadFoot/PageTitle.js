

export const PageTitle = ({title}) => {
    return (
        <>
           <div className="page-header">
                <h2 className="page-title">{title}</h2>
            </div> 
        </>
    )
}